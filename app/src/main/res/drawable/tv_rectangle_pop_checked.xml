<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners
        android:radius="@dimen/space_5" /> <!-- 设置四个角的半径 -->
    <solid android:color="@color/rb_pop_background" /> <!-- 定义填充的颜色值 -->

    <!-- 边框 -->
    <stroke
        android:width="0.5dp"
        android:color="@color/rb_pop_background" />
</shape>